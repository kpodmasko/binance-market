const testIds = {
    app: 'app',
    header: 'header',
    select: {
        root: 'select',
        option: 'option'
    },
    icon: {
        root: 'icon',
        instance: 'icon__instance'
    }
}

export {
    testIds
}